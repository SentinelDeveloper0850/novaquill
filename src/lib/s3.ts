// S3 storage has been replaced by Cloudinary.
// This file is intentionally kept as a no-op compatibility stub until older imports disappear from every deployment cache.

export function isS3Enabled(): boolean {
  return false;
}
